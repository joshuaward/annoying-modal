import React from 'react';

const HiddenButton = () => {
	return (
		<div>
			<button className="hidden-trigger">
				<i className="fas fa-fire"></i>
			</button>
		</div>
	);
};

export default HiddenButton;